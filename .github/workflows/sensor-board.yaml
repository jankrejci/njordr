name: sensor-board-gerbers

on:
  pull_request:
    paths:
    - 'sensor-board/*.kicad_sch'
    - 'sensor-board/*.kicad_pcb'
    - 'sensor-board/*.kibot.yaml'
    - '.github/workflows/sensor-board.yaml'
  workflow_dispatch:
    inputs:
      name:
        description: 'Workflow run name'
        required: false
        default: 'sensor-board gerbers'
  
jobs:
  jlcpcb:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: INTI-CMNB/KiBot@v2_k9
      with:
        config: jlcpcb.kibot.yaml
        dir: output
        schema: 'sensor-board/sensor-board.kicad_sch'
        board: 'sensor-board/sensor-board.kicad_pcb'
    - name: 'Cleanup build folder'
      run: sudo rm -rf ./output/tmp
    - name: 'Upload results'
      uses: actions/upload-artifact@v4
      with:
        name: sensor-board-jlcpcb
        path: output
