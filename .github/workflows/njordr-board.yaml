name: njordr-board-gerbers

on:
  pull_request:
    paths:
    - 'njordr-board/*.kicad_sch'
    - 'njordr-board/*.kicad_pcb'
    - 'njordr-board/*.kibot.yaml'
    - '.github/workflows/njordr-board.yaml'
  workflow_dispatch:
    inputs:
      name:
        description: 'Workflow run name'
        required: false
        default: 'njordr-board gerbers'
  
jobs:
  jlcpcb:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: INTI-CMNB/KiBot@v2_k9
      with:
        config: jlcpcb.kibot.yaml
        dir: output
        schema: 'njordr-board/njordr-board.kicad_sch'
        board: 'njordr-board/njordr-board.kicad_pcb'
    - name: 'Cleanup build folder'
      run: sudo rm -rf ./output/tmp
    - name: 'Upload results'
      uses: actions/upload-artifact@v4
      with:
        name: njordr-board-jlcpcb
        path: output
